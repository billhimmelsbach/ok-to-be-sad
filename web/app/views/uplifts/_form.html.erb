<%= form_for(@uplift) do |f| %>
  <% if @uplift.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@uplift.errors.count, "error") %> prohibited this uplift from being saved:</h2>

      <ul>
      <% @uplift.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :url %><br>
    <%= f.text_field :url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
